# KidsHub Monorepo Restructure Plan

Tracking doc for the restructure of the `daycares/` monorepo into three clearly-scoped apps.

## Decisions locked in

- **Mobile + web stack for `kidshub`:** Expo (React Native) + React Native Web — one codebase, native iOS/Android + web.
- **Repo strategy:** Single monorepo at `daycares/`.
- **Order of work:** `kidshub-landing` → `kidshub-dashboard` → `kidshub`.

## Decisions resolved

- [x] Monorepo root: **flattened** — `kidshub-app/*` moved up into `daycares/`.
- [x] Workspace tool: **npm workspaces** (plain, no Turborepo for now).
- [x] Lockfile strategy: **single root `package-lock.json`**; per-app lockfiles removed.

## Open decisions (resolved 2026-04-21 night, before Phase 3 kickoff)

- [x] **Styling on mobile: NativeWind.** Existing dashboard + legacy kidshub are heavily Tailwind, classes port ~1:1. Lower-risk than Tamagui; can migrate later if perf walls hit.
- [x] **Firebase on mobile: Firebase JS SDK.** One code path across web + iOS + Android, simpler EAS builds. When push notifications become important, swap **just** the messaging layer to `@react-native-firebase/messaging` without disturbing the data layer.
- [x] **Keep `kidshub-legacy/` snapshot.** `git mv` it instead of deleting — quick local reference during port, removed from workspaces so it doesn't pull deps. Deleted after Phase 3 parity confirmed (p3-19).
- [x] **Expo monorepo: stay on npm workspaces + Metro config.** No pnpm migration. If Metro chokes on hoisting, fix with `metro.config.js` resolver tweaks rather than swapping the package manager.

---

## Target state

```
daycares/
├── kidshub/                 # unified parent + teacher, web + mobile (role-based, Expo + RN Web)
├── kidshub-dashboard/       # owner-only, web-only (renamed from kidshub-owner, React + Vite)
└── kidshub-landing/         # marketing site, web-only (static HTML/CSS/JS + Vercel /api)
```

---

## Phase 0 — Monorepo scaffolding ✅

- [x] **p0-1** Flatten monorepo — move `kidshub-app/*` up into `daycares/` (preserve `.git` history).
- [x] **p0-2** Add root `package.json` with npm workspaces for the 3 apps.
- [x] **p0-3** Add root `.gitignore`, `.editorconfig`, `README.md`, `.vscode` workspace file.
- [x] **p0-4** Commit clean baseline before any app-level changes.
- [x] **p0-5** Consolidate to a single root `package-lock.json`, remove per-app lockfiles + `node_modules/`.

---

## Phase 1 — `kidshub-landing` ✅

**Goal:** Public marketing site, web-only, clearly scoped.

- [x] **p1-1** Audit `index.html` to confirm it's marketing-only (no app logic). Also cleaned up the orphaned `js/chat.js` serverless handler (merged Resend lead-email flow into `api/chat.js`), added CORS origin allowlist, generated placeholder `assets/favicon.svg`.
- [x] **p1-2** Flesh out `package.json` (name, scripts `dev`/`build`/`deploy`).
- [x] **p1-3** Add `vercel.json` pinning static site + `/api` routes + security headers.
- [x] **p1-4** ~~Update CTAs to point at `kidshub` app and `kidshub-dashboard`.~~ _No-op — landing page is intentionally lead-capture-only; no sign-in links. Confirmed 2026-04-21._
- [x] **p1-5** Write `README.md` (purpose, deploy target, local dev).
- [x] **p1-6** Deploy to Vercel and smoke-test `/api/chat` + `/api/test`. Live on `getkidshub.com`, Claude 3 Haiku via OpenRouter responding in ~3s.

### Bonus scope — Aria hardening (added in Phase 1, not originally planned)

- [x] **p1-7** Fix Aria 400 — removed invalid Anthropic-direct model ID from client, pinned model server-side in `api/chat.js`.
- [x] **p1-8** Cost + isolation — dedicated OpenRouter account for KidsHub, $10 per-key spend cap, pinned to `anthropic/claude-3-haiku` (~$2.50 / 1000 conversations).
- [x] **p1-9** Layer 1 defense — Origin/Referer allowlist check in `/api/chat`, rejects curl/bot traffic without spoofed headers (403).
- [x] **p1-10** Layer 2 defense — request body validation: max 20 messages, 2000 chars/message, 4000 chars/system prompt, 500 chars/lead field, hard `max_tokens` ceiling of 500. Runs pre-upstream so bogus requests cost zero credit.
- [ ] **p1-11** Layer 3 defense — per-IP rate limiting via Upstash Redis. Optional polish; $10 spend cap already limits blast radius.

---

## Phase 2 — `kidshub-dashboard` (rename from `kidshub-owner`) ✅

**Goal:** Owner-only web app, web-only, stays React + Vite.

- [x] **p2-1** Rename folder `kidshub-owner` → `kidshub-dashboard`. `git mv` preserved history on all 58 tracked files; untracked `.env` moved intact. Root `package.json` workspaces + scripts (`dev:dashboard`/`build:dashboard`), `daycares.code-workspace`, and root `README.md` updated in the same commit. Commit: `4988fcc`.
- [x] **p2-2** Update `package.json` name (`kidshub-owner` → `kidshub-dashboard`) + added product description. Replaced 13 fake-staff `@kidshub-owner.com` mock-email references with IANA-reserved `@example.com` (correct for mock data per RFC 2606). Lockfile rebuilt cleanly (0 `kidshub-owner` refs remain outside the plan doc's historical notes). Build verified green.
- [x] **p2-3** Audit pages — classification matrix below, ready to drive Phase 3 extraction.

  All 12 pages in `kidshub-dashboard/src/pages/` reviewed. Classifications:

  | Page | Audience today | Phase 3 destination | Notes |
  | --- | --- | --- | --- |
  | `Login.jsx` | Owner auth | Keep in dashboard; `kidshub` gets its own role-aware login | Different flow: kidshub login routes to `(parent)` or `(teacher)` based on Firestore user role |
  | `Register.jsx` | Owner self-signup | Keep in dashboard; `kidshub` gets parent-only register | Teachers join via owner-invite flow (p3-14), not self-register |
  | `Dashboard.jsx` | Owner overview (stats across classrooms, staff, parents) | **Owner-only — stays** | Teacher "home" in kidshub is a different component scoped to their classroom; parent "home" is scoped to their child |
  | `Children.jsx` | All roles, different scopes | **Port to `kidshub`** (both groups) | Owner: all children + add. Teacher: classroom-scoped, read-only. Parent: own child only. Same component with scope prop, or three thin wrappers over a shared list |
  | `ChildProfile.jsx` | Teacher + parent | **Port to `kidshub`** (both groups) | Teacher creates logs (meal/nap/diaper/photo/note); parent reads timeline. Gate quick-log actions behind role check |
  | `Classrooms.jsx` | Owner (manage classrooms + assign staff/children) | **Owner-only — stays** | Teachers see only their own classroom (embedded in teacher home), don't need the list view |
  | `Staff.jsx` | Owner (manage teacher roster) | **Owner-only — stays** | Teachers don't manage other staff |
  | `Messages.jsx` | All roles | **Port to `kidshub`** (both groups); owner keeps moderate view in dashboard | Parent ↔ teacher DMs scoped by childId |
  | `Schedule.jsx` | All roles, different edit rights | **Port to `kidshub`** (both groups) | Teacher: edit their classroom schedule. Parent: read-only of their child's classroom |
  | `CheckIn.jsx` | Teacher-primary | **Port to `kidshub`** `(teacher)` | Parents see check-in status on their ChildProfile; they don't actually check kids in |
  | `Activities.jsx` | Teacher-primary | **Port to `kidshub`** `(teacher)` | Parents read activities via ChildProfile timeline; they don't log them |
  | `Settings.jsx` | All roles, scoped | **Split** in Phase 3 | Personal (profile/notifications/theme) → kidshub shared. Business (building info/billing/plan) → stays in dashboard |

  Shared components also destined for kidshub port in Phase 3 (p3-12): `Layout`, `Header`, `Sidebar`, `Notifications`, all `ui/*` atoms, `ActivityIcons`, `ChildCard`, `HighlightNoteBox`, `QuickLogBar`, and scoped versions of `AddChildModal` (teacher flow may differ from owner flow).

  Net: **6 pages port to `kidshub`** (Children, ChildProfile, Messages, Schedule, CheckIn, Activities), **5 stay** (Login, Register, Dashboard, Classrooms, Staff), **1 splits** (Settings).
- [x] **p2-4** Strengthen `ProtectedRoute` to owner-only + friendly redirect for non-owners. Ended up being the whole role system since it didn't exist:

  - New `src/constants/roles.js` defines `ROLES` enum (`owner` / `teacher` / `parent`), `ROLE_LABELS` for UI, and `DASHBOARD_ALLOWED_ROLES = [ROLES.OWNER]` as the dashboard's allowlist. Single source of truth — will be lifted to a shared package if/when the Phase 3 `kidshub` app needs the same values.
  - `AuthContext` now loads the Firestore `users/{uid}` doc alongside the Firebase Auth session via `onSnapshot`, exposes `profile`, `role`, and role-predicate helpers (`isOwner`, `isTeacher`, `isParent`). Subscription auto-cleans on sign-out or unmount. Fails closed on Firestore rule rejection.
  - `ProtectedRoute` now takes an `allowedRoles` prop (default `DASHBOARD_ALLOWED_ROLES`). Unauthenticated → `/login`. Authenticated + no profile doc → `/unauthorized` with `reason: 'no-profile'`. Authenticated + disallowed role → `/unauthorized` with `reason: 'wrong-role'`.
  - New `pages/Unauthorized.jsx` — on-brand bounce page with three distinct messages (no-profile, wrong-role, unknown), sign-out button, link to `getkidshub.com`, alt-account sign-in link. Parent/teacher copy teases the upcoming `kidshub` app.
  - `Register.jsx` now writes `role: ROLES.OWNER` instead of a magic string.
  - **Security posture:** this is client-side defense-in-depth. The authoritative gate is Firestore security rules (Phase 3 p3-15). Anyone with devtools can bypass the client check — so the rules **must** verify `request.auth.uid`'s role server-side before allowing reads/writes of other tenants' data.
- [ ] **p2-5** Update `.env.example`, rotate any committed secrets.
  - **Audit finding (2026-04-21):** `.env.example` files are clean (placeholder values only). `src/firebase/config.js` is already fully env-driven in both `kidshub-dashboard/` and `kidshub/`. **However** commit `810abbd` left real Firebase web config values in git history — `kidhub-7a207` project, api key `AIzaSyCxt-...`. Firebase web keys are client-side by design, not server secrets, so this isn't an instant breach — but needs hardening:
  - [x] **p2-5a** _(user, dashboard)_ Locked the Google Cloud Console API key with Website restrictions: `getkidshub.com`, `*.getkidshub.com`, `*.vercel.app`, `localhost`, `127.0.0.1` (2026-04-21). Note: the new GCP "Websites" UI rejects port (`:*`) and path (`/*`) wildcards, so bare domains were used — they match across any scheme/port/path anyway. API restrictions left at "25 APIs" (Firebase default, already appropriately scoped). Key still public in git history but now unusable from unauthorized origins.
  - [x] **p2-5b** Firestore Security Rules audit + fix. **Rules were wide open** — Firebase default test rules allowed `read, write: if request.time < 2026-05-06` (no auth required, world-readable until May 6). With the project ID in git history, this was a one-line script away from total data loss/exfiltration. Wrote tightened ruleset at repo root `firestore.rules` covering the current flat-collection data model: `users/{uid}` gated to self + role immutable post-creation (prevents self-promotion), `centers/{ownerId}` gated to the matching owner only, flat business collections (`children`/`classrooms`/`staff`/`activities`/`messages`/`parents`/`announcements`) gated to any authenticated owner via `isOwner()` helper, default-deny catch-all. Extensively commented with the pilot-era caveat that multi-tenancy (owner-A can't see owner-B's data) still isn't enforced — that waits for Phase 3 p3-15 when the data model is refactored to add `centerId` or nested subcollections. Source-of-truth now lives in git; user deployed by pasting into the Firebase Console for now, CI-driven `firebase deploy` in Phase 4.
  - [ ] **p2-5c** _(optional)_ Rotate the Firebase web API key and update `VITE_FIREBASE_API_KEY` in Vercel env vars for all three apps. Don't bother rewriting git history (destructive, doesn't scrub GitHub caches).
- [x] **p2-6** Write `README.md` (owner portal purpose, deploy, local dev). Covers tech stack, access-control model (role gating + defense-in-depth caveat), full folder layout, local dev + seed flow, Firestore data model (including the `users.role` vs `staff.role` trap), env vars with the Firebase-keys-aren't-secrets nuance, deploy steps, and a p2-7 Vercel post-rename checklist for the user to execute.
- [x] **p2-7** Reconfigured Vercel project to `kidshub-dashboard`, deployed to `kidshub-owner.vercel.app` (Vercel keeps the original auto-assigned URL after rename — harmless), added custom domain `dashboard.getkidshub.com` via GoDaddy CNAME → `cname.vercel-dns.com`. SPA rewrites + security headers shipped via `kidshub-dashboard/vercel.json` (commit `2d36547`). DNS propagated in <10 min, Vercel auto-issued SSL cert, owner-flow smoke test green.

---

## Phase 3 — `kidshub` (biggest lift: unify parent + teacher, Expo + RN Web)

**Goal:** Single Expo app serving parents and teachers with role-based routing, shipping to iOS, Android, and Web.

### 3a. Bootstrap

- [x] **p3-1** Backed up existing `kidshub/` → `kidshub-legacy/` via `git mv` (28 files, history preserved). Removed from npm workspaces (root `package.json`) — won't pull deps on `npm install`. Root scripts updated: `dev:kidshub` now exits with a helpful message pointing at the legacy run instructions; `build:kidshub` removed entirely. `daycares.code-workspace` updated to surface `kidshub-legacy` as a clearly-labeled "frozen reference" folder. Root `README.md` rewritten to reflect current 4-folder reality with status flags (✅/🚧/🧊). Lockfile rebuilt cleanly.
- [x] **p3-2** Bootstrapped Expo SDK 54 (latest stable) at `kidshub/` via `npx create-expo-app@latest --template default --no-install`. Stack landed: Expo SDK 54.0.33, React 19.1, React Native 0.81.5, Expo Router 6.0 (typed routes + React Compiler experiments enabled), TypeScript 5.9, eslint-config-expo. Came with `app/(tabs)/`, `_layout.tsx`, `modal.tsx` — proper Expo Router file-based routing. Default template intentionally chosen over `blank` so we get realistic `_layout`/route group examples to learn the patterns from before deleting them in p3-9. Added back to root npm workspaces with hoisted install (1045 packages).
- [x] **p3-3** Enabled React Native Web (already in template at v0.21) and verified web works. Two infra issues hit + fixed:
  - **Node version mismatch:** Metro/Expo SDK 54 uses `Array.prototype.toReversed` (Node 20+ only). Project was on Node 18.20.8 → bump to 20.19.5. Added `.nvmrc` (`20.19.5`) and `engines: { node: ">=20.0.0" }` in root `package.json` so onboarding `nvm use` auto-picks the right version.
  - **Workspace hoisting collision:** root had `semver@6.3.1` hoisted (an old transitive of webpack/etc); `react-native-reanimated@4.x` requires the modular API (`semver/functions/satisfies`) only available in semver v7+. With Metro's `disableHierarchicalLookup` it couldn't see the v7 nested deeper. Fixed by `npm install --workspace kidshub semver@^7.6.0` — installs v7.7.4 into `kidshub/node_modules/semver`, Metro finds it via local `nodeModulesPaths`. Added `metro.config.js` with the standard Expo monorepo template (watchFolders includes monorepo root, both nodeModulesPaths registered, hierarchical lookup disabled for determinism).
  - Smoke test: `expo start --web --port 5174` → HTTP 200, 30 KB rendered output, RN-Web stylesheet (`css-view-*`, `r-borderColor-*` classes) confirms RN-Web is bundling correctly. Metro bundled 1146 modules cleanly with React Compiler enabled.
- [x] **p3-4** Configured `app.json` for cross-platform shipping: display name `KidsHub` (proper casing), slug `kidshub`, scheme `kidshub://` for deep links, **`ios.bundleIdentifier` and `android.package` both pinned to `com.getkidshub.app`** (matches the marketing domain — clean for App Store + Play Store discoverability). Web bundler explicitly set to Metro (`web.bundler: "metro"`); web output stays `static` (Vercel-deployable). Kept `newArchEnabled: true` (RN's New Architecture is default in SDK 54) and `experiments: { typedRoutes, reactCompiler }` from the template.

### 3b. Foundations

- [x] **p3-5** Wired Firebase JS SDK + EXPO_PUBLIC env vars.
  - `kidshub/firebase/config.ts` — initializeApp guarded by getApps().length===0 (hot-reload safe), exports `auth`, `db`, `storage`, default app. Uses `process.env.EXPO_PUBLIC_FIREBASE_*` (Expo's convention for client-exposed vars; same kidhub-7a207 project as kidshub-dashboard / kidshub-landing — Phase 4 p4-2 will lift to a shared workspace package).
  - `kidshub/.env` — copied from `kidshub-dashboard/.env` with VITE_FIREBASE_* → EXPO_PUBLIC_FIREBASE_* via sed.
  - `kidshub/.env.example` — committed checked-in template with the same EXPO_PUBLIC_* keys + comments explaining Expo conventions and why the Firebase web key isn't a secret (referrer-locked in GCP).
  - `kidshub/constants/roles.ts` — TypeScript port of `kidshub-dashboard/src/constants/roles.js`. Same `ROLES` enum, but `KIDSHUB_ALLOWED_ROLES = [PARENT, TEACHER]` (owners belong on dashboard). Phase 4 p4-2 lifts the duplicate.
  - **Auth persistence note**: `getAuth(app)` works fine on web (IndexedDB) and renders/builds clean on native, but on iOS/Android it'll warn "no persistence" and default to in-memory (logout on app restart). Proper RN persistence (`initializeAuth` + `getReactNativePersistence` + AsyncStorage) deferred to **p3-16** (mobile build config) since we're testing on web first.
  - **babel-preset-expo cleanup along the way**: when installing `nativewind`, npm pulled in babel-preset-expo@55 which Expo SDK 54 flagged as incompatible. Tried explicit pin in `kidshub/package.json` → npm wrote v55 anyway because some transitive forced it. Resolved with a root-level `overrides: { "babel-preset-expo": "~54.0.10" }` in the monorepo `package.json`, removed the (now-redundant) explicit dep from `kidshub/package.json`, and confirmed via `npm ls babel-preset-expo --all` that the only resolved copy is `expo@54.0.33 → babel-preset-expo@54.0.10 overridden`. Warning gone from `expo start` output.
  - Smoke test: home screen shows two NativeWind cards — pink "NativeWind smoke test" and green "Firebase project: kidhub-7a207". `curl localhost:5178/` returns the project ID + brand colors in the bundled HTML, confirming env loading + Firebase init work end-to-end.
- [x] **p3-6** Ported AuthContext + added ThemeContext for the kidshub Expo app.
  - `kidshub/contexts/AuthContext.tsx` — TS port of `kidshub-dashboard/src/contexts/AuthContext.jsx`. Same two-subscription pattern: `onAuthStateChanged(auth)` chained into `onSnapshot(users/{uid})` for live role/profile. Exposes `user`, `profile`, `role`, `isAuthenticated`, `isOwner`/`isTeacher`/`isParent`, `login`, `logout`, `resetPassword`, `loading`. Fails-closed on profile read errors (role=null → ProtectedRoute will bounce in p3-13). Legacy kidshub used a one-shot `getDoc(parents/{uid})` — we're going with the dashboard's live-snapshot pattern because role changes (teacher invite accepted, parent onboarded) need to propagate without a full logout.
  - `kidshub/contexts/ThemeContext.tsx` — new piece. Three-state preference (`'system' | 'light' | 'dark'`) layered on top of `useColorScheme` from `react-native`. Exposes `preference`, `setPreference(p)`, `effective` ('light' | 'dark' — what to actually render). In-memory only for MVP; AsyncStorage persistence will land with the same install that adds RN auth persistence in p3-16.
  - `kidshub/contexts/index.ts` — barrel export for `{ AuthProvider, useAuth, ThemeProvider, useTheme }` and their types.
  - `kidshub/app/_layout.tsx` — wired provider tree: `<ThemeProvider><AuthProvider><NavigationThemeBridge>…`. The bridge component consumes our ThemeContext's `effective` value and hands React Navigation the appropriate DarkTheme/DefaultTheme so header/tab chrome flip with user preference. Aliased RN's `ThemeProvider` import as `NavigationThemeProvider` to avoid collision with ours.
  - Smoke tests on home screen (4 cards now — pink/green/purple/blue): AuthContext card shows `signed out` / `signed in (role: X)` / `loading…` depending on state, and ThemeContext card has three Pressable pills (`system` / `light` / `dark`) that swap the `effective` value live. Verified via `curl`: `bg-accent-600`, `bg-info-600`, `AuthContext: <!-- -->loading…` all present in served HTML, meaning both providers mount cleanly during SSR without throwing the "useXxx must be used within an XxxProvider" guard.
- [x] **p3-7** Seeded `kidshub/hooks/` with the navigation + auth scaffolding the page ports (p3-9 through p3-13) will need.
  - **Legacy had zero custom hooks** — react-router-dom primitives were used inline. So p3-7 is really "write the hooks we'll need so porting is mechanical", not "port existing hooks".
  - Kept the template's theme helpers (`use-color-scheme`, `use-theme-color`) as-is.
  - Added three:
    - `use-is-route-active.ts` — replaces RRD's `<NavLink>` active-class pattern by wrapping `usePathname()`. Exact-match default; opt-in `{ exact: false }` for "path OR any nested child" matches. Used by BottomNav/Sidebar ports in p3-12.
    - `use-auth-redirect.ts` — one-liner for auth-sensitive screens. Two modes: `{ require: 'authenticated', redirectTo: '/login' }` bounces anons out; `{ require: 'anonymous', redirectTo: '/' }` keeps signed-in users off /login. Waits for `AuthContext.loading` so there's no flicker-bounce.
    - `use-require-role.ts` — layered on top. Takes an allow-list of roles, redirects to `/login` if anon, `/unauthorized` if signed-in-but-wrong-role. Defense-in-depth with Firestore rules (p3-15 is the real gate).
  - `hooks/index.ts` — barrel so `@/hooks` imports don't care about internal layout.
  - `hooks/README.md` — full RRD → expo-router migration map table. Pinned reference so page ports in p3-10/p3-11 don't require re-thinking per-file (useNavigate → useRouter, useLocation → usePathname + useLocalSearchParams, useParams → useLocalSearchParams, Navigate → Redirect, NavLink → useIsRouteActive, &lt;Routes&gt; → file tree). This doc is the p3-7 deliverable as much as the hooks themselves are.
  - Final smoke test: all 4 context cards (pink/green/purple/blue) render, `curl` confirms `bg-brand-500` / `bg-success-500` / `bg-accent-600` / `bg-info-600` / `kidhub-7a207` / `loading…` all present in served HTML. No babel-preset-expo warning. Phase 3b foundations done.
- [x] **p3-8** Installed NativeWind v4 + Tailwind 3.4. Six-file setup per the official Expo guide:
  - `tailwind.config.js` with full color palette ported from `kidshub-legacy/tailwind.config.js` (brand pink ramp #FFF0F7→#8F0040, accent purple, surface slate, success/warning/danger/info, plus Inter font family). NB: dropped the `boxShadow` and `keyframes`/`animation` extensions — RN doesn't honor CSS box shadows or CSS keyframes the same way; we'll use `react-native-reanimated` for animations and platform-aware shadow styles in p3-12 if we miss them. Content globs cover `app/`, `components/`, `hooks/`, `constants/`.
  - `global.css` — three Tailwind directives only (`@tailwind base/components/utilities`).
  - `babel.config.js` — `babel-preset-expo` with `jsxImportSource: 'nativewind'` + `nativewind/babel` preset chained.
  - `metro.config.js` — extended the existing monorepo config (watchFolders + nodeModulesPaths + disableHierarchicalLookup) by wrapping the final export in `withNativeWind(config, { input: './global.css' })`.
  - `nativewind-env.d.ts` — single triple-slash directive for type completion. NativeWind installer auto-added it to `tsconfig.json` includes.
  - `app/_layout.tsx` — added `import '../global.css'` at the top so the stylesheet is in the bundle from app boot.
  - Pinned `babel-preset-expo@~54.0.10` (npm initially installed v55 which Expo SDK 54 flagged as incompatible).
  - Visual smoke test: dropped a `<View className="bg-brand-500 p-4 rounded-xl my-2">` banner on the home screen, verified `bg-brand-500` compiled to `rgb(255 45 138)` (= #FF2D8A, our brand pink), `font-bold`/`p-4`/`rounded-xl`/`text-white` all present in served HTML. Banner gets removed in p3-9 when we replace the screen wholesale.

### 3c. Routing + UI

- [x] **p3-9** Built the routing shell — role-based groups, role router, wrong-role landing.
  - **Deleted template demos**: `app/(tabs)/` (entire group — `index`, `explore`, `_layout`) and `app/modal.tsx`. The smoke-test home screen from p3-5 through p3-7 went with them, as flagged.
  - **Trimmed `app/_layout.tsx`**: removed `unstable_settings.anchor: '(tabs)'` and the two demo `Stack.Screen` entries. Now just a headerless `<Stack />` under the provider tree — each group owns its own layout chrome.
  - **`app/index.tsx` (role router, the ONLY `/` route)**: dispatches based on AuthContext state. `loading` → `<ActivityIndicator/>` + "Loading KidsHub…" copy; anon → `<Redirect href="/login"/>`; parent role → `/home`; teacher role → `/classroom`; anything else (owner, missing, invalid role) → `/unauthorized`. Reason this file exists: groups in `()` don't consume URL segments, so two `index.tsx` files under `(parent)` and `(teacher)` would collide at `/`. Distinct landing paths per role + a dispatcher at `/` is the clean pattern.
  - **`app/unauthorized.tsx`**: explanation + sign-out button. Owners get an extra "Go to dashboard" link to `dashboard.getkidshub.com` (web only; `Platform.OS === 'web'` guard so native RN doesn't try to render a web-anchor `Link`).
  - **`(auth)` group** — `_layout.tsx` calls `useAuthRedirect({ require: 'anonymous', redirectTo: '/' })` (signed-in users can't get stuck on /login), then `<Stack headerShown: false>`. Three stub screens: `login.tsx`, `register.tsx`, `forgot-password.tsx`. Each carries a yellow "Stub — p3-9 scaffolding" card pointing at the phase that'll port the real form (p3-10 or p3-13). Cross-links between the three so navigation is verifiable.
  - **`(parent)` group** — `_layout.tsx` calls `useRequireRole({ allowedRoles: [ROLES.PARENT] })`, then `<Tabs>` pulling `HapticTab` + `IconSymbol` from the Expo template (still useful, not yet replaced in p3-12). One tab for now: `home.tsx` → `/home`. Layout comment lists the follow-ups p3-10 will add (schedule, activity, messages, photos, profile).
  - **`(teacher)` group** — mirror of `(parent)`, `useRequireRole([ROLES.TEACHER])` + `<Tabs>` with a single `classroom.tsx` → `/classroom`. p3-11 adds attendance / activity / messages / photos / profile tabs.
  - **Route overlap note**: both groups will eventually have a `messages.tsx` mapping to `/messages`. Expo Router resolves these by "active group" — a parent navigating from /home to /messages hits `(parent)/messages.tsx`; a teacher hits `(teacher)/messages.tsx`. No collision at runtime as long as the role router sends each role into its own group's entry path.
  - **Smoke test (anonymous state, web SSR)**: all 6 routes return 200 and render their distinct fingerprints:
    - `/` → `Loading KidsHub…` (router in loading state during SSR, will Redirect on client)
    - `/login` → `KidsHub` + `Sign in to connect` + `Create an account` + `Forgot password`
    - `/register` → `Create account` + `Already have an account`
    - `/unauthorized` → `You're in the wrong place` + `Sign out`
    - `/home` → `Welcome back` + `Stub` (SSR renders the page; client useEffect will bounce anons to /login)
    - `/classroom` → `Your classroom` + `Stub` (same SSR behavior)
    No errors, no babel warnings. Phase 3c shell is stable.
- [x] **p3-10** Ported all 6 parent pages (`Home`, `Schedule`, `Activity`, `Messages`, `Photos`, `Profile`) to `(parent)` group as real functional screens (not stubs). Each screen reads from `kidshub/data/mockData.ts` (typed port of legacy mock data — will be swapped for Firestore in p3-15).
  - `kidshub/app/(parent)/home.tsx` — greeting, high-priority announcement banner, ChildStatusCard (colored stripe + avatar + check-in state + quick stats), Recent Activity preview (4 items, links to /activity), Quick Actions 2×2 grid (Messages / Daily Report / Photos / Schedule).
  - `kidshub/app/(parent)/activity.tsx` — child header, NapStatusCard (expo-linear-gradient info gradient; shown only when a nap is in progress), horizontal filter chips (All / Meals / Naps / Activities / Diaper), full timeline list. Legacy's snack-in-Meals-filter special case preserved.
  - `kidshub/app/(parent)/schedule.tsx` — classroom header + current time, vertical timeline with 3 state variants (completed/current/upcoming), connector lines between dots via plain `<View>` (RN can't use pseudo-elements), "Now" badge + highlight row on the current item, legend row at bottom.
  - `kidshub/app/(parent)/messages.tsx` — teacher header (avatar + online dot), scrolling message list with auto-scroll-to-bottom via `ScrollView` ref, gradient bubble for "from me" messages, attachment preview placeholder, bottom composer with `KeyboardAvoidingView` (iOS) + `TextInput multiline` + gradient Send button. `scrollIntoView` → `scrollToEnd`, `textarea Enter-to-send` → tap-only (standard mobile chat UX). Image picker deferred to p3-15.
  - `kidshub/app/(parent)/photos.tsx` — grouped-by-date thumbnails (2-col grid, `aspectRatio: 1`), tap opens full-screen RN `<Modal>` with prev/next chevrons, empty-state card when no photos. Skipped legacy's Heart/Download header buttons (no-op stubs) and hover caption overlay (web-only). Modal caption shows full date + time.
  - `kidshub/app/(parent)/profile.tsx` — hero card (avatar + name + classroom dot + allergy badges), "Child Information" section (Medical, Emergency Contacts, Authorized Pickups, Schedule, Documents), "Settings" section (Notifications, Privacy, Help), Sign Out card with spinner. Detail routes for each row intentionally deferred — this release just needs the shell.
  - Tabs wired: `kidshub/app/(parent)/_layout.tsx` now registers all 6 tabs (Home → Activity → Schedule → Messages → Photos → Profile) with lucide-react-native icons (swapped away from `IconSymbol` — SF/Material platform-specific glyphs looked inconsistent; lucide gives a single visual family matching legacy). Role guard + `RouteSplash` gate preserved from p3-13.
  - Smoke tests on `localhost:5190`: all 11 routes (4 unauth + 6 parent + /unauthorized) return HTTP 200. Bundle downloaded (11.5 MB, 2906 modules) and grepped — every ported string present (`Daily Schedule`, `Activity Feed`, `Currently Napping`, `Chat with teachers`, `Type a message`, `Manage your child`, `Emergency Contacts`, `Authorized Pickups`, `photos from daycare`, `Sunshine Room`, `Recent Activity`, `Quick Actions`, `Morning Circle Time`). No compile errors, no babel warnings.
- [x] **p3-11** Ported 5 teacher-facing screens from `kidshub-dashboard` into the `(teacher)` group. Same mock-data-first pattern as p3-10 — Firestore wiring deferred to p3-15 so the two roles swap to live reads in a single consolidated change.
  - `kidshub/data/mockData.ts` — extended with teacher-side fixtures: `classrooms[]` (Sunshine + Rainbow rooms with color + age range), `staff[]` (lead teacher Sarah Mitchell + assistant Michael Rodriguez, both in classroom-sunshine), `classroomRoster[]` (6 children — Ava Singh first so parent and teacher see the same entity during local testing; Liam Patel + Mason Kim are absent so the attendance split is 4-in-2-out by default), `classroomActivities[]` (~12 seeded log entries across breakfast → outdoor → lunch → nap so type filters and the timeline have real range to exercise), and `teacherConversations[]` (4 threads with unread counts, matching the dashboard's `conversations/{id}` + `messages[]` shape for zero-cost migration).
  - `kidshub/app/(teacher)/classroom.tsx` — teacher landing tab, port of dashboard's `Children.jsx` scoped to one classroom. Dropped the grid/list view toggle (phone-first — grid is the only legible mode), the "Add Child" button (owner action, not teacher), and the three filter selects (overkill for a 15-kid classroom). Kept the roster card layout + attendance summary strip + quick-action buttons (Check in / Log activity) linking to `/check-in` and `/activities`.
  - `kidshub/app/(teacher)/check-in.tsx` — port of dashboard's `CheckIn.jsx`. Live `useChildrenData` hook → local `useState` seeded from `classroomRoster`; tapping Check-in/Out flips the child's status + updates `checkInTime`. Web `<input>` / `<textarea>` → RN `<TextInput>`; the centered dialog became a slide-up bottom-sheet `<Modal>` with `KeyboardAvoidingView`. Allergy alerts surface both on the card and inside the modal. Stats strip (In/Not in/Total) + search + filter chips (All/In/Not in) all preserved.
  - `kidshub/app/(teacher)/activities.tsx` — port of dashboard's `Activities.jsx` with the full 3-step logging flow (type → child → notes). 19 activity types collapsed to 13 loggable ones (dropped `checkin`/`checkout` — those go through the Check-in tab; dropped `mood`/`play`/`music`/`health` — types exist in the map but aren't teacher-initiated yet). Quick-log chip row ported as horizontal `ScrollView` (plays better with phone widths than the flex-wrap grid). Modal opens from a FAB-style button OR from tapping a chip (chip pre-selects the type, skipping Step 1). `activitiesApi.create(...)` → local append. Classroom filter dropped; already scoped.
  - `kidshub/app/(teacher)/messages.tsx` — port of dashboard's `Messages.jsx`, restructured for phone. Dashboard's two-pane desktop layout (list + thread side-by-side) swapped for a list→thread navigation stack (tapping a conversation opens the full thread with a back button). Reuses the same gradient bubble + `KeyboardAvoidingView` composer from `(parent)/messages.tsx` so both roles share the same chat UX. Unread count badges per row, marked-as-read on open. Deferred: attachments, typing indicators, read receipts, conversation search.
  - `kidshub/app/(teacher)/teacher-profile.tsx` — teacher's own profile. Hero card (avatar + name + email + classroom dot), "My classroom" section (children count, co-teachers list, classroom documents), Settings (Notifications / Privacy / Help), Sign out card mirroring the parent profile flow (`AuthContext.logout()` → `router.replace('/login')`). Route name is `teacher-profile` not `profile` because both groups would otherwise claim `/profile`; even though Expo Router's group resolution would pick the right one at runtime, sharing a URL segment between two role-specific screens made `router.push('/profile')` from shared components brittle.
  - `kidshub/app/(teacher)/_layout.tsx` — wired all 5 tabs (Classroom → Check in → Activities → Messages → Profile) with lucide-react-native icons (`Home`, `UserCheck`, `Activity`, `MessageSquare`, `User`). Same `useRequireRole({ allowedRoles: [ROLES.TEACHER] })` + `RouteSplash` gate preserved from p3-9.
  - Skipped for this ticket: `Schedule` (teacher read-only view isn't materially different from parent's — will share the same route once Firestore lands), `ChildProfile` detail (deep-link from Classroom, not a tab — defers to p3-15 where teacher can create activity logs via the same `activitiesApi.create` path).
  - Smoke tests on `localhost:5191` (restarted with `--clear` to pick up new routes): all 14 routes (4 unauth + 6 parent + 4 teacher + /unauthorized) return HTTP 200. Teacher bundle strings verified via grep: `Manage daily attendance`, `Log activity`, `Select activity type`, `Today's roster`, `Parent conversations`, `My classroom`, `Co-teachers`, `Allergy alert`, `Sunshine Room`, plus all 6 roster children names present. Bundle 11.5 MB / 2876 modules. No compile errors, no babel warnings.
- [x] **p3-12** Ported shared components (`layout`, `ui`, `icons`) to RN. Subsumed into p3-10 session because the parent pages can't render without them.
  - `kidshub/components/ui/card.tsx` — `Card` / `CardHeader` / `CardBody`. Legacy's `card` / `card-hover` CSS classes don't exist on RN, so they were re-expressed as explicit NativeWind utilities (bg-white dark:bg-surface-800, rounded-2xl, border, shadow-sm). `hover` prop accepted for API parity but no-op on native.
  - `kidshub/components/ui/avatar.tsx` — initials-on-brand-bg circle OR image source. Legacy's `avatar-sm` / `avatar-md` / `avatar-lg` / `avatar-xl` CSS classes replaced with explicit pixel sizes (32/40/48/64) to kill the CSS dependency.
  - `kidshub/components/ui/badge.tsx` — re-expressed 7 semantic variants (brand/accent/success/warning/danger/info/neutral) as bg/text class pairs. Auto-wraps string children in `<Text>` so callers can mix icons + plain text without tripping RN's "text must be in `<Text>`" rule.
  - `kidshub/components/icons/activity-icon.tsx` — activityIcons map (19 activity types → lucide-react-native components), activityColors map (bg class + icon hex stroke color, since lucide-rn doesn't respect NativeWind text-* classes for stroke), activityLabels map, `ActivityIcon` component with 3 size variants.
  - `kidshub/components/layout/screen-container.tsx` — lightweight replacement for legacy `<Layout>`. Skipped porting `Sidebar`/`Header`/`BottomNav` entirely because Expo Router's `<Tabs>` in `(parent)/_layout.tsx` already owns the bottom nav and a desktop sidebar isn't appropriate for a native-tabbed app. `ScreenContainer` just provides the per-screen title/subtitle header block + safe-area-aware ScrollView. Scrollable by default; `scrollable={false}` mode for fixed-chrome screens like Messages.
  - `expo-linear-gradient` installed (SDK-pinned via `expo install`) for the gradient send button + from-me message bubble + nap status card.

### 3d. Auth + data

- [x] **p3-13** Implemented real login + register + forgot-password screens, plus hardened the route guards.
  - `kidshub/contexts/AuthContext.tsx` — extended with `registerParent({ firstName, lastName, email, password, phone })`. Three steps in one method: `createUserWithEmailAndPassword` → `updateProfile(displayName)` → `setDoc(users/{uid}, { role: 'parent', childIds: [], status: 'active', ... })`. Writes go to `users/{uid}` (the source-of-truth doc the dashboard reads), NOT `parents/{uid}` like legacy did — keeps role checks single-source. Teachers do NOT self-register here; the dashboard's invite flow (p3-14) seeds their `users/{uid}` doc with `role: 'teacher'` + `daycareId`.
  - `kidshub/firebase/errors.ts` — small mapper from Firebase Auth error codes (`auth/invalid-credential`, `auth/email-already-in-use`, `auth/too-many-requests`, etc.) to friendly user-facing copy. Falls back to a generic message + console.error for unknown codes so dev tools still see the raw error.
  - `kidshub/app/(auth)/login.tsx` — proper email/password form. KeyboardAvoidingView + ScrollView for small viewports. Lucide icons (Mail/Lock prefix, Eye/EyeOff toggle suffix). Disabled state while submitting, error banner, "Forgot password?" link, "Create one" link to /register. After success, AuthContext flips → (auth) layout's redirect → role router → `/home` (parent) or `/classroom` (teacher).
  - `kidshub/app/(auth)/register.tsx` — 2-step parent self-signup mirroring legacy UX. Step 1 collects name/email/phone; Step 2 collects password + confirm with show/hide toggles and minimum-6-char validation. Pink dots step indicator. Error banner uses the same friendly mapper. Calls `registerParent`; success path is identical to login.
  - `kidshub/app/(auth)/forgot-password.tsx` — its own route now (legacy had it as a toggle inside Login). Two visual states: form → success card. Treats `auth/user-not-found` as success too (anti-enumeration); copy says "If an account exists for X, we've sent…" rather than confirming the email is registered.
  - `kidshub/hooks/use-require-role.ts` + `use-auth-redirect.ts` — both hooks now expose a `status: 'loading' | 'allowed' | 'redirecting'` so layouts can gate render until the guard resolves. Fixes a one-frame flicker where, before `router.replace()` lands, a wrong-role user could glimpse the protected tab bar.
  - `kidshub/components/route-splash.tsx` — neutral full-bleed `<ActivityIndicator>` in brand pink. Used by `(auth)`, `(parent)`, `(teacher)` layouts AND the role router (`app/index.tsx`) so all four loading states look identical.
  - `kidshub/app/(auth|parent|teacher)/_layout.tsx` — each layout now early-returns `<RouteSplash />` while `status !== 'allowed'`, then renders the Stack/Tabs. Defense-in-depth on top of Firestore security rules (p3-15 will lock things down server-side; this just keeps UX clean).
  - **Defense-in-depth chain:** UI guard (`useRequireRole` in layout, gated by `status`) → Firestore security rules (p3-15) → server side will reject the query even if a malicious client bypasses the UI. UI guard exists for UX (clean redirect to `/unauthorized` instead of a screen full of "permission denied" errors).
  - Smoke tests on `localhost:5181`: all 7 routes return HTTP 200; guarded routes (`/`, `/login`, `/register`, `/forgot-password`, `/home`, `/classroom`) all SSR to the same 38693-byte splash (because `AuthContext.loading=true` server-side); `/unauthorized` is the only route that SSRs its full content (38792 bytes) since it has no guard. Bundled JS contains every form's strings, all lucide icons (3406 references), the friendly error mapper, the 2-step register indicator, the anti-enumeration copy, and the new `registerParent` / `createUserWithEmailAndPassword` / `updateProfile` / `serverTimestamp` calls. No compile errors, no module-resolution failures.
  - **Deferred:** persistent auth on native (RN AsyncStorage) is still p3-16 — fine for web-first, but every cold start on iOS/Android currently signs the user out. Phase 3d will fix this alongside EAS build setup.
- [x] **p3-14** Teacher invite flow (owner invites teacher in dashboard → teacher accepts in kidshub `/invite/[token]` → `users/{uid}` gets `role: 'teacher'` + `classroomId` + `daycareId`).
  - **Data model.** New `invites/{token}` collection. Doc ID is the URL-unguessable token (`crypto.randomUUID()`); fields are `email` (locked, teacher must register w/ exactly this), `role: 'teacher'`, `classroomId`, `classroomName` (denormalized for accept screen), `daycareId` (= `ownerId` in pilot), `invitedBy` (= owner uid, enforced by rule), `invitedByName` (denormalized), `createdAt`, `expiresAt` (created + 7 days).
  - **Firestore rules.** `invites/{token}`: open read (token IS the unguessable capability), create only by owners with `invitedBy == request.auth.uid` and `role == 'teacher'`, delete by inviter (revoke) or invitee email (consume). Updates forbidden — revoke + reissue if details change. `users/{uid}` create rule extended to allow `role: 'teacher'` ONLY when the new doc's `inviteToken` field references a real `invites/{token}` doc whose `email == request.auth.token.email` and `role == 'teacher'`. So teachers can never self-promote without a real invite. Expiry enforced client-side (Firestore can't easily compare server-set timestamps to `request.time` without overhead, and the only reader is the prospective teacher whose URL it is).
  - **Dashboard side.** New `firebase/api/invites.js` (create / getByToken / delete / `subscribeForOwner` for the live-updating Pending Invites list, plus `generateInviteToken` helper). New `InviteTeacherModal.jsx` is a 2-stage modal: form (email + classroom select) → success view with `${VITE_KIDSHUB_APP_URL || 'http://localhost:5191'}/invite/{token}` shown copy-to-clipboard with a `Check`-pill confirmation. Augmented `Staff.jsx` with an "Invite teacher" header button (mobile + desktop variants), and a "Pending invites" `Card` section above the staff grid that subscribes via `invitesApi.subscribeForOwner(user.uid)` and renders each invite with email, classroom (color-dot + name), expiry date, copy-link button (per-row "Copied!" pill via `copiedToken` state — 2s timeout per row so clicking row B doesn't clear row A's pill), and a destructive trash-icon revoke button (with `window.confirm` guard). Files: `kidshub-dashboard/src/firebase/api/invites.js` (new, ~165 lines), `firebase/api/index.js` (export), `components/modals/InviteTeacherModal.jsx` (new, ~300 lines), `components/modals/index.js` (export), `pages/Staff.jsx` (PendingInvites sub-component, "Invite teacher" header button, mobile button, modal mount).
  - **Kidshub side.** `AuthContext.tsx` extended with `fetchTeacherInvite(token): Promise<TeacherInvite | null>` and `acceptTeacherInvite({ token, firstName, lastName, password })`. Acceptance is: re-fetch invite (defensive, in case revoked between page load + submit) → check `expiresAt` → `createUserWithEmailAndPassword(invite.email, password)` (using the locked email so `request.auth.token.email` matches the rule) → `updateProfile(displayName)` → `setDoc(users/{uid}, { role: 'teacher', classroomId, daycareId, inviteToken, invitedBy, ... })` → `deleteDoc(invites/{token})` (best-effort; logged-not-thrown if it fails — the email is now registered so the token is unusable anyway). Wraps the whole flow in the same `setRegistering(true)` mask used by `registerParent` so the role router never briefly sees `role=null` between auth-state-flip and `setDoc` resolving. New types `TeacherInvite` + `AcceptTeacherInviteInput` exported from `contexts/index.ts`.
  - **Accept screen.** New `app/invite/[token].tsx` — outside any group so anonymous teachers can reach it. Branches: loading spinner → InvalidInvite card (token gone or expired) → SignOutToContinue card (signed in as someone else, with explicit sign-out CTA — no auto sign-out, could trash unsaved work in another tab) → AcceptForm (firstName/lastName/password/confirm with the same KeyboardAvoidingView + lucide icon-prefixed inputs as `(auth)/register.tsx`). Already-accepted edge: if the signed-in user's email matches the invite's email AND their role is already `'teacher'`, render `<Redirect href="/" />` (the role router will send them to `/classroom`). On submit, `auth/email-already-in-use` is surfaced specially as "ask your daycare to revoke + reissue" rather than the generic friendly-error mapper output.
  - **Edge cases handled.** Missing token in URL → fetchError. Token doesn't exist (deleted/revoked) → InvalidInvite. Expired (`expiresAt.toMillis() < Date.now()`) → InvalidInvite with "ask for a new one" copy. Different account signed in → SignOutToContinue (with CTA). Same teacher already accepted → silent redirect to `/`. `auth/email-already-in-use` → contextual error (don't auto-merge; that's a security risk if a parent account exists on this email). Concurrent revoke between page load + submit → `acceptTeacherInvite` re-fetches and throws "no longer valid" with friendly copy.
  - **Smoke tests on `localhost:5191` + `localhost:5173`.** All 6 kidshub routes return HTTP 200 (`/`, `/login`, `/register`, `/home`, `/classroom`, `/invite/{any-token}`). Dashboard `/staff` HTTP 200. SSR for `/invite/abc123` renders the loading state (expected; client-side fetch resolves the token). End-to-end manual smoke test by user: owner created invite → copied URL → opened in incognito → filled name + password → submitted → landed on `/classroom` showing the teacher tab bar. Pending invites section auto-cleared when the invite was consumed.
  - **Files.** `firestore.rules` (`invites/{token}` block + extended `users/{uid}` create rule, +69 lines), `kidshub-dashboard/src/firebase/api/invites.js` (new), `kidshub-dashboard/src/firebase/api/index.js` (export), `kidshub-dashboard/src/components/modals/InviteTeacherModal.jsx` (new), `kidshub-dashboard/src/components/modals/index.js` (export), `kidshub-dashboard/src/pages/Staff.jsx` (PendingInvites sub-component + invite UI), `kidshub/contexts/AuthContext.tsx` (acceptTeacherInvite + fetchTeacherInvite + types), `kidshub/contexts/index.ts` (type exports), `kidshub/app/invite/[token].tsx` (new), `kidshub/app/_layout.tsx` (route table comment update). Plus this doc.
  - **Deferred to follow-up.** (1) Email delivery — invites are copy-to-clipboard URLs today; the owner DMs/emails them out-of-band. Wiring this to a transactional email provider (Resend or SendGrid via a `kidshub-landing` `/api/send-invite` route) is a low-risk follow-up. (2) Teacher record linking — accepting an invite creates `users/{uid}` but doesn't backfill `staff/{id}` from the dashboard. Phase 4 cleanup will merge these (or just deprecate the legacy `staff/` collection in favor of `users` where role='teacher'). (3) Invite TTL is hardcoded to 7 days in `invitesApi.create`; could make it owner-configurable.
- [ ] **p3-15** Update Firestore security rules for role-scoped queries (`classroomId` for teachers, `childIds` for parents).

  **Audit (existing state — 2026-04-21):**

  | Collection | Current scoping field(s) | Used by | Notes |
  | --- | --- | --- | --- |
  | `users/{uid}` | `role`, teacher-only: `classroomId` + `daycareId`, parent-only: `childIds[]` (always `[]` today — no link mechanism) | both apps | Owner doc lacks an explicit `daycareId`; **convention: `daycareId === ownerUid`** (enshrined by `InviteTeacherModal` passing `user.uid` as `daycareId`). |
  | `invites/{token}` | `email`, `role`, `classroomId`, `daycareId`, `invitedBy` | dashboard → kidshub | Done in p3-14. |
  | `centers/{ownerId}` | doc ID = `ownerId` | dashboard | Already scoped 1:1 to owner. |
  | `children/{id}` | `classroom` (FK string, note: not `classroomId`), `status`, `checkInTime` | dashboard only today | No `daycareId`, no `parentIds[]`. |
  | `classrooms/{id}` | none | dashboard only today | No `daycareId`. |
  | `staff/{id}` | `classroom`, `status` | dashboard only today | No `daycareId`. Separate from `users where role='teacher'` — merge deferred to p4. |
  | `activities/{id}` | `childId`, `staffId`, `type`, `timestamp` | dashboard only today | No `classroomId`, no `daycareId`. |
  | `messages/{id}` | `senderId`, `senderType`, `recipientId`, `childId`, `conversationId` | dashboard only today | No `daycareId`. |
  | `parents/{id}` | `children[]` | dashboard only | Legacy sibling of `users where role='parent'`. Merge deferred to p4. |
  | `announcements/{id}` | `timestamp` | dashboard only | No `daycareId`, no audience scoping. |

  **Data-model backfills needed on dashboard API writes (part of this ticket):**
  - `children.create` — stamp `daycareId: auth.currentUser.uid`, initialize `parentIds: []`.
  - `classrooms.create` — stamp `daycareId`.
  - `staff.create` — stamp `daycareId`.
  - `activities.create` — stamp `daycareId` + `classroomId` (derived from `childId → children.classroom` at write time).
  - `messages.create` — stamp `daycareId` at write time.
  - `announcements.create` — stamp `daycareId`.
  - `users.register` (owner) — stamp `daycareId: user.uid` on the owner's own users doc (so `get(/users/{uid}).data.daycareId` works uniformly for all roles in rules).

  These are all additive fields. Existing legacy docs without `daycareId` will be tolerated by rules during the transition (falls back to owner-any-doc behavior for the matching doc). A one-off backfill script/button is deferred — pilot has near-zero live business data today, so a clean-slate is fine.

  **Access matrix (target state):**

  Legend: `-` = no access, `R` = read, `W` = write (create/update), `Rs` = read scoped, `Ws` = write scoped, `own` = only their own doc.

  | Collection | Owner | Teacher | Parent | Anon |
  | --- | --- | --- | --- | --- |
  | `users/{uid}` | R any in tenant; W to update `childIds` on parents in tenant | R/W own (role frozen) | R/W own (role frozen) | - |
  | `invites/{token}` | W create (self as inviter); R any (token is the capability); Delete own invites | R/Delete if `auth.email == resource.email` | same as teacher | R any (URL = capability) |
  | `centers/{ownerId}` | R/W if `ownerId == auth.uid` | - | - | - |
  | `children/{id}` | R/W (tenant) | Rs where `classroom == teacher.classroomId`; Ws narrow (check-in/out only — status + timestamps) | Rs where `id in parent.childIds` | - |
  | `classrooms/{id}` | R/W (tenant) | Rs single doc where `id == teacher.classroomId` | Rs where `daycareId == parent.daycareId` (requires parent to have `daycareId` stamped — set when owner links first child) | - |
  | `staff/{id}` | R/W (tenant) | Rs where `daycareId == teacher.daycareId` (co-teachers view) | - | - |
  | `activities/{id}` | R/W (tenant) | Rs where `classroomId == teacher.classroomId`; Ws create with `classroomId == teacher.classroomId` + `staffId == teacher.uid` | Rs where `childId in parent.childIds` | - |
  | `messages/{id}` | R/W (tenant) | Rs where `senderId == auth.uid` OR `recipientId == auth.uid`; Ws create with `senderId == auth.uid` + `senderType == 'staff'`; Ws update `read` on own received | same as teacher but `senderType == 'parent'` | - |
  | `parents/{id}` (legacy) | R/W (tenant) | - | - | - |
  | `announcements/{id}` | R/W (tenant) | Rs where `daycareId == teacher.daycareId`; Ws create with `daycareId == teacher.daycareId` | Rs where `daycareId == parent.daycareId` | - |

  **Multi-tenant `daycareId` isolation is intentionally scoped OUT of this ticket.** Owner-any-doc rules for business collections remain (pilot-scale, 1–3 owners, all trusted). Adding `resource.data.daycareId == ownerDaycareId()` to owner rules is a purely additive follow-up (p3-15b or p4-x) — all writes are already being stamped with `daycareId`, so the rule change is one line per collection whenever we're ready to flip the switch.

  **Parent ↔ child linkage (how `childIds` gets populated):**
  - Today: no mechanism. Parents register with `childIds: []` and stay there.
  - p3-15 adds: an owner-only "Link parent to child" action in the dashboard (on the child detail / parents page). Writes to both `children/{id}.parentIds` (append) and `users/{parentUid}.childIds` (append). Rules allow an owner to update `childIds` + `daycareId` on a parent's users doc (explicitly whitelisted fields — can't bulk-overwrite the parent's profile).
  - Scoped out: parents-invite-themselves / self-service claiming. Owners approve all linkages, keeps PII safe.

  **Order of operations for the implementation sub-tasks:**
  1. Add `daycareId`-stamping + required derived fields to dashboard API helpers (p3-15c below). Deploy to dashboard in dev.
  2. Rewrite `firestore.rules` with the matrix above, keeping legacy-tolerant fallbacks (p3-15d). Paste into Firebase Console.
  3. Smoke-test dashboard (owner) + kidshub (parent + teacher) paths with live data (p3-15e).
  4. Wire live data into kidshub screens (swap mock data → Firestore). **Intentionally not this ticket** — that's a separate follow-up once rules are known-good; we don't want rule bugs masked by still-mocked UIs.

- [x] **p3-15a** Audit existing collections + queries + fields (this section above).
- [x] **p3-15b** Design access matrix (this section above).
- [x] **p3-15c** Backfill `daycareId` + `classroomId` + `parentIds` on dashboard API writes.

  Every `create()` in `kidshub-dashboard/src/firebase/api/*.js` now stamps `daycareId: auth.currentUser.uid` and any derived field the new rules require:
  - `children.create` — `daycareId` + `parentIds: []` (initialized for future parent linkage).
  - `classrooms.create`, `staff.create`, `messages.create`, `parents.create`, `announcements.create` — `daycareId`.
  - `activities.create` — `daycareId` + `classroomId` (derived from the child doc via `getDoc(children/{childId})` if the caller didn't pass it; accepts both `classroomId` and legacy `classroom` field names).
  - `Register.jsx` (owner self-signup) — stamps `daycareId: user.uid` on the new `users/{uid}` doc so every role's daycareId lookup uses the same rule helper (`userDoc().daycareId`).
  - New `users.js` API with `linkParentToChild(parentUid, childId)` + `unlinkParentFromChild()` — writes both `users/{parentUid}.childIds` and `children/{childId}.parentIds` in a `writeBatch` so the two sides of the relationship stay in lockstep. Rule-gated to owners linking parents in their own tenant.

  Each helper reads from `auth.currentUser?.uid` via a local `currentDaycareId()` function; throws early with a clear error if called while unauthenticated (rather than letting the rule silently reject). New `usersApi` exported from `firebase/api/index.js`.

- [x] **p3-15d** Rewrite `firestore.rules` with role-scoped + multi-tenant matrix.

  **Strict-mode enforcement** — every business doc must carry `daycareId`. Legacy docs without it become unreadable under the new ruleset. This is the clean-slate call documented at the top of the rules file: pilot has near-zero live business data, so the churn is contained. If the user has test data they want to keep, the recovery path is a one-off Firebase Console backfill (stamp `daycareId = ownerUid` on every doc).

  **Helper function layout:**
  - `isAuthenticated()` / `isSelf(userId)` / `hasProfile()` — cheap checks.
  - `userDoc()` — single `get(/users/{auth.uid})` per rule eval, Firestore-deduped across multiple calls → one read cost.
  - `userRole()` / `userDaycare()` / `userClassroom()` / `userChildIds()` — field accessors with safe defaults (`get(key, default)`) so rule doesn't throw on missing fields (e.g. a parent doesn't have `classroomId`).
  - `isOwner()` / `isTeacher()` / `isParent()` — existence-guarded role checks.
  - `inMyTenant(data)` — `data.daycareId == userDaycare()`. The workhorse predicate; appears in nearly every rule.
  - `childClassroom(data)` — accepts both the canonical `classroomId` and the legacy `classroom` field name on children docs, to future-proof the planned rename.

  **Key rule shapes:**
  - `users/{uid}` create — role-specific gating: owner must set `daycareId == userId`, parent must NOT set `daycareId` (owner seeds it later), teacher must match the invite's `daycareId` + `classroomId` on top of the p3-14 email/role checks.
  - `users/{uid}` update — self branch freezes `role` / `uid` / `daycareId` / `childIds` / `classroomId` / `inviteToken` / `invitedBy` via `affectedKeys().hasAny([...])` check (allows profile edits but no privilege-escalation surface). Owner branch is narrowly scoped to linking a parent: only `{childIds, daycareId, updatedAt}` may change, only on `role=='parent'` users, only if the target's existing `daycareId` is either unset or already equals the owner's uid (prevents "steal parent" attacks).
  - `children` update — owner is unrestricted within tenant; teacher may only update check-in/out fields (`status` / `checkInTime` / `checkOutTime` / `lastCheckIn*` / `lastCheckOut*` / `updatedAt`) on children in their classroom, and cannot flip `daycareId`.
  - `activities` create — owner stamps `daycareId == own uid`; teacher stamps `daycareId == own daycareId` + `classroomId == own classroomId` + `staffId == own uid` (three-way lock: can't attribute activity to another teacher, another classroom, or another tenant).
  - `messages` — read only by sender/recipient within tenant; create must stamp `senderId == auth.uid` + `daycareId == own daycareId` (prevents impersonation and cross-tenant injection); update restricted to `{read, readAt}` when recipient, unrestricted for owner.
  - `announcements` — read by anyone in tenant; create by owner or teacher stamping own daycareId; update/delete owner only.

  **Deferred (intentional):**
  - Parent discovery UX for linkParentToChild (today the owner needs the parent's UID out-of-band). Proper solution is a parent-claim flow (daycare code or owner-initiated link request + parent approval). Separate ticket.
  - Merging legacy `parents/{id}` + `staff/{id}` collections into `users where role=X`. These coexist today because "employee roster" (staff) and "teacher with app login" (users) are actually different product concepts — an owner can employ a teacher without granting app access. Phase 4 cleanup.
  - Wiring kidshub teacher + parent screens off mock data onto live Firestore queries. Kept out of this ticket so rule bugs show up in clean tests, not masked by half-swapped UIs.

- [ ] **p3-15e** Smoke test: dashboard still works (owner); kidshub invite accept still works (teacher); kidshub parent signup still works.

### 3d-bis. Parent invite flow (p3-20, added 2026-04-21)

**Gap p3-20 closes:** at end of p3-14/p3-15 we have teachers joining via owner-issued invite, but parents still only self-signup → dormant account with no linkage. The product won't feel real until a parent clicks a link from the daycare and immediately sees their child. This adds the parent-side mirror of the teacher invite flow.

**Shape:**
- Owner opens a child's profile in the dashboard → "Invite parent to app" button → email + token → copyable URL.
- Parent pastes URL in kidshub → signs up with that email → `users/{uid}` is stamped with `role='parent'`, `daycareId=owner.uid`, `childIds=[childId]`, `inviteToken`; their uid is `arrayUnion`'d into `children/{childId}.parentIds`; invite doc deleted.
- Parent lands on `/home` with the child read rule already unlocked (`childId in userChildIds()` AND `uid in parentIds`).

**Rules added (firestore.rules):**
- `invites/{token}` create — generalized to accept `role='teacher'` (classroomId) OR `role='parent'` (childId), still gated on `isOwner()` + `invitedBy == auth.uid` + `daycareId == auth.uid`.
- `users/{uid}` create — new `role='parent' + inviteToken` branch that requires `daycareId == invite.daycareId`, `childIds` is a list containing `invite.childId`, and `invite.email == request.auth.token.email`.
- `children/{childId}` update — new `isParent()` self-link branch that allows a parent to arrayUnion themselves into `parentIds` (only — no other fields touched, no existing parent links removed via a subset check).

**Why parent can update `children.parentIds` client-side:** the alternative is a Cloud Function to close the link server-side, but that's a whole deploy pipeline we don't have yet. The subset check (`resource.data.parentIds.toSet().difference(request.resource.data.parentIds.toSet()).size() == 0`) prevents a malicious parent from evicting other parents. Scope-limited attack surface: only a parent already linked to this child can even reach this branch.

- [x] **p3-20a** Extend `invites/{token}` + `users/{uid}` + `children/{id}` update rules for parent invite + self-link.
- [x] **p3-20b** Extend dashboard `invitesApi` with `createParentInvite` + `subscribeForChild`; add `InviteParentModal`.
- [x] **p3-20c** Wire "Invite parent to app" button + pending-invites list into `ChildProfile` Contacts tab (copy / revoke).
- [x] **p3-20d** Allow parent to `arrayUnion` self into `children.parentIds` on accept (merged into p3-20a rules).
- [x] **p3-20e** kidshub: generalize `fetchInvite` + add `acceptParentInvite` in `AuthContext`; branch `/invite/[token]` on role for copy + submit handler.
- [ ] **p3-20f** Smoke test: owner creates parent invite → parent pastes URL → signup → lands on `/home` → child appears in parent screens (mock data still fronts parent views until p3-15e live-data wire).

### 3e. Build + deploy

- [ ] **p3-16** Configure EAS Build (`eas.json`) for iOS + Android.
- [ ] **p3-17** Configure web deploy for RN-Web output (Vercel or Expo hosting).
- [ ] **p3-18** Smoke-test on iOS simulator, Android emulator, and web.
- [ ] **p3-19** Delete `kidshub-legacy/` once parity is confirmed.

---

## Phase 4 — Cross-cutting cleanup

- [ ] **p4-1** Update root `README.md` with final 3-app layout + run commands.
- [ ] **p4-2** Consolidate Firebase config to env-driven single source of truth.
- [ ] **p4-3** Add GitHub Actions CI running `build` per workspace on PRs.
- [ ] **p4-4** Set up 3 Vercel projects + domain plan (`kidshub.com`, `app.kidshub.com`, `dashboard.kidshub.com`).

---

## Progress log

_Append dated notes as phases complete._

- **2026-04-21 (afternoon — CRUD parity)** — Full CRUD parity shipped across the dashboard so every entity that can be added can now be edited and deleted (crud-0 → crud-3c). Built a single shared `ConfirmDialog` primitive (`components/ui/ConfirmDialog.jsx`) that handles the submitting/error/blocked states every delete flow needs, then threaded it through the pages uniformly. Pattern for every create modal was: rename `AddXModal` → `XFormModal`, accept an optional `entity` prop, branch `handleSubmit` between `.create` and `.update`, keep `AddXModal` as a back-compat alias. Applied to Classroom, Child, and Staff. Edit/Delete buttons appear as hover-revealed row actions on list cards (`stopPropagation` on the inner buttons so clicking them doesn't navigate) and as explicit action-bar buttons on detail screens. User chose the `block_with_deps` policy for safety: deletes that would orphan other records are refused with a human-readable reason shown inside the confirm dialog — classroom with children/staff, child with linked parents, etc. For children specifically we *do* cascade activities + messages because they're log data that would otherwise leak into the next child with the same ID; the cascade goes through `childrenApi.deleteWithDependents` which batches refs 400-at-a-time to stay under Firestore's write limits. Wave 2 (parent unlink) needed its own UI because the legacy `parentsApi` "contacts on file" collection and the new `users/{uid}` parent accounts are both displayed in the ChildProfile Contacts tab — added an "App-linked parents" section that fetches `users/{uid}` by id for each `child.parentIds` entry and offers a Remove button per row, wired to `usersApi.unlinkParentFromChild` (which already does the two-sided `arrayRemove`). Wave 3 added per-row trash on Activities + per-bubble trash on Messages (with a preview of the deleted content in the confirm message), and a brand-new `/announcements` page with full CRUD: `AnnouncementFormModal` for create/edit (title + body + priority + audience targeting), `Announcements.jsx` list page with row actions, sidebar nav entry (`Megaphone` icon, between Messages and Schedule), and the route wired in `App.jsx`. The existing Firestore rules already allowed owner update/delete within their own tenant, so no rules changes were needed — the entire wave was client-side. Files: `ConfirmDialog` + barrel; `AddChildModal`, `AddClassroomModal`, `AddStaffModal` (all gained edit mode + `XFormModal` exports); `AnnouncementFormModal` (new); `Classrooms.jsx`, `Children.jsx`, `ChildProfile.jsx`, `Staff.jsx`, `Activities.jsx`, `Messages.jsx` (all gained edit/delete wiring); `Announcements.jsx` (new); `Sidebar.jsx`, `App.jsx`; `childrenApi.deleteWithDependents` added. Still open: `crud-4` end-to-end smoke test with the dev server.

- **2026-04-21 (mid-day)** — p3-14 ✅ teacher invite flow shipped end-to-end. Owner creates invite in dashboard Staff page → copy URL → teacher pastes in incognito → fills name + password → lands on `/classroom` with `users/{uid}.role='teacher'` + `classroomId` + `daycareId` set, invite doc auto-deleted. Two security wrinkles fixed before getting here: (a) `firestore.rules` only allowed `role: 'owner'` on `users/{uid}` create — extended to allow `role: 'parent'` (self-signup) and `role: 'teacher'` (only with a matching invite token whose email == `request.auth.token.email`); (b) registration race where `onAuthStateChanged` fired before `setDoc(users/{uid})` resolved — the role router briefly saw `role=null` and bounced the user to `/unauthorized`. Fixed at root with a `registering` boolean in `AuthContext` that masks `loading=true` for the duration of `registerParent` + `acceptTeacherInvite`, plus a defensive `useEffect` in `/unauthorized` that auto-redirects if a valid role appears post-mount (self-heal in case the race ever resurfaces). `registerParent` also now self-heals "orphan auth users" (auth account exists but no Firestore profile, leftover from when the rules blocked parent role) by catching `auth/email-already-in-use`, signing in with the provided creds, and writing the missing profile doc. Invite tokens are `crypto.randomUUID()` (122 bits of entropy, URL-safe), 7-day TTL, single-use (deleted on accept). Email delivery deferred — owner copies the URL and sends it out-of-band today. Files: `firestore.rules`, `kidshub-dashboard/src/firebase/api/invites.js` (new), `InviteTeacherModal.jsx` (new), `Staff.jsx` (PendingInvites + Invite button), `kidshub/contexts/AuthContext.tsx`, `kidshub/app/invite/[token].tsx` (new), `kidshub/app/unauthorized.tsx` (race auto-heal). End-to-end smoke test passed by user. Next: p3-15 — Firestore role-scoped rules so a teacher in classroom A literally cannot read a child in classroom B (today's rules let any authenticated owner read any business doc; teachers/parents can't read business docs at all yet, which means teacher screens will break the moment they swap from mock data to live reads).

- **2026-04-21 (pre-dawn)** — Phase 3c teacher pages ✅ (p3-11 closed). Ported 5 teacher screens — Classroom, Check in, Activities, Messages, Profile — from `kidshub-dashboard/src/pages/*.jsx` to `kidshub/app/(teacher)/*.tsx`. Same mock-data-first pattern as p3-10: added `classrooms[]`, `staff[]`, `classroomRoster[]` (6 children, Ava Singh first so parent + teacher see the same entity), `classroomActivities[]` (~12 seeded entries spanning today's timeline), and `teacherConversations[]` (4 threads) to `data/mockData.ts`. Dashboard's desktop-table layouts re-thought for phone-first: the grid/list toggle on Children became grid-only; the two-pane Messages split became a list→thread navigation; filter chips became horizontal scrollers; modals became slide-up bottom sheets with `KeyboardAvoidingView`. `activitiesApi.create` + `childrenApi.checkIn/Out` calls replaced with local `useState` mutations so the UI flows work without Firestore (swaps back to live reads in p3-15 — the shapes are identical). Skipped `Schedule` (teacher read-only ≈ parent view; defer until a shared component makes sense) and `ChildProfile` (detail route, not a tab — owner-class feature). Tab icons continue lucide-react-native for consistency with the parent layout. Route name `teacher-profile` (not `profile`) to keep URL space unambiguous between groups. 14 routes (4 unauth + 6 parent + 4 teacher + /unauthorized) all HTTP 200 on `localhost:5191`; teacher bundle strings verified via grep (`Manage daily attendance`, `Select activity type`, `Today's roster`, `Parent conversations`, `My classroom`, `Co-teachers`, `Allergy alert`, `Sunshine Room`, all 6 roster names present). 11.5 MB / 2876 modules, no compile errors. Files: 5 new teacher screens + teacher mock data expansion + updated `(teacher)/_layout.tsx` + this doc. Next: p3-13 teacher invite flow or p3-15 Firestore wiring to swap both roles to live data simultaneously.

- **2026-04-21 (deep night)** — Phase 3c parent pages ✅ (p3-10, p3-12 closed together). Ported all 6 parent screens — Home, Activity, Schedule, Messages, Photos, Profile — from `kidshub-legacy/src/pages/*.jsx` to `kidshub/app/(parent)/*.tsx`, alongside the shared UI primitives they depend on (Card, Avatar, Badge, ActivityIcon) and a slimmed-down `ScreenContainer` that replaces legacy's full `Layout` (Expo Router Tabs own the bottom nav, so Sidebar/Header/BottomNav weren't ported — they'd just be dead code). Typed `data/mockData.ts` carries the same shape as legacy's JS so the Firestore swap in p3-15 is a purely mechanical rewrite of the data source, not the screens. Cross-platform web-isms neutralized: `<img>` → `<Image>`, `<button>` → `<Pressable>` (with `active:` for mobile-style press feedback, since `hover:` is web-only), `<Link to>` → `expo-router` `<Link href>`, `scrollIntoView` → `scrollToEnd` via ref, Tailwind gradients → `expo-linear-gradient`, lucide-react → `lucide-react-native`. Tab bar switched from `IconSymbol` (SF Symbols iOS / Material Android / mismatched visuals) to straight lucide-react-native icons so every platform ships the same glyph family and the UI looks like the legacy design. 11 routes (4 unauth + 6 parent + /unauthorized) all HTTP 200 on `localhost:5190`; 11.5 MB bundle (2906 modules) grepped clean for every ported screen's identifier strings. No compile errors, no babel warnings. Files touched: 7 new screens/components + 1 mock-data file + 3 barrel exports + 1 tab layout update + this doc. Deferred: parent detail screens (per-row Profile navigations), image picker in Messages, real Firestore-backed data (all tracked in p3-14/15).

- **2026-04-21 (after midnight)** — Phase 3a (bootstrap) ✅. p3-1: legacy snapshot via `git mv kidshub kidshub-legacy` (28 files, history preserved), removed from workspaces (commit `8fd2d75`). p3-2/3/4: Expo SDK 54 bootstrapped, RN-Web verified, app.json configured. Two infrastructure landmines defused along the way: (1) Node 18.20 → 20.19 forced by Metro's use of `toReversed` (now pinned via `.nvmrc` + engines field — onboarding `nvm use` will auto-switch); (2) `semver@6` hoisting clash with reanimated@4's need for the v7 modular API, fixed by installing v7 into the `kidshub` workspace directly. Added monorepo-aware `metro.config.js` (watchFolders + nodeModulesPaths + disableHierarchicalLookup — the standard Expo monorepo template). Bundle IDs pinned to `com.getkidshub.app` for both iOS + Android (matches marketing domain). Web smoke test green: HTTP 200, RN-Web CSS classes present, 1146 modules bundled. Decisions locked in: NativeWind for styling (Tailwind classes port ~1:1), Firebase JS SDK for one-codepath data layer, `kidshub-legacy/` kept as reference until p3-19 parity check, npm workspaces over pnpm. Phase 3b (Firebase + contexts + NativeWind) starts next session.

- **2026-04-21** — Phase 0 complete. Flattened `kidshub-app/*` into `daycares/`, added root `package.json` (npm workspaces), `.editorconfig`, `daycares.code-workspace`, extended `.gitignore` for Expo/RN. Removed stale per-app `package-lock.json` files and nested `node_modules/`; root `npm install` produced a single hoisted tree (261 MB, down from 542 MB) with one canonical `package-lock.json`. Commits: `21ad22f` (scaffolding), `2f16764` (lockfile consolidation).
- **2026-04-21** — Phase 1 (code) complete. Audited `kidshub-landing` and confirmed marketing-only (no Firebase, auth, or leaked secrets). Added `dev`/`dev:vercel`/`build`/`deploy` scripts so `npm run dev:landing` works from the monorepo root. Merged Resend lead-email flow from an orphaned `js/chat.js` into the live `api/chat.js`, added CORS origin allowlist, generated a brand favicon at `assets/favicon.svg`. Added `vercel.json` with security headers and cache policy. Rewrote `kidshub-landing/README.md` for monorepo context and current env-var names. p1-4 (sign-in CTAs) confirmed as a no-op per product decision. Commits: `fb30a3a` (landing scripts), `ad7e014` (Aria harden + dead-code removal), pending (vercel.json + README). p1-6 (Vercel deploy + smoke test) handed off to user via Vercel dashboard.
- **2026-04-21 (evening)** — Phase 1 fully ✅. Aria was returning 400s on prod (the widget was sending `claude-haiku-4-5-20251001`, an Anthropic-direct model ID invalid on OpenRouter); the old handler had hidden this by hardcoding the model. Pinned model server-side (`ceef18b`). Chased a subsequent free-tier 429 through Llama 3.3 70B and a fallback chain (`ef27f26`, `3d3efb3`), ultimately choosing paid `anthropic/claude-3-haiku` for uptime (`829ecd9`) — ~$0.0025 per conversation is negligible for a lead widget. Created dedicated OpenRouter account for KidsHub, rotated key into Vercel env vars, set $10 per-key spend cap. Hardened `/api/chat` against abuse: Origin/Referer rejection (Layer 1) + request shape + size caps + `max_tokens` ceiling (Layer 2) — all running pre-upstream so bogus requests cost zero. Commit `7ec406d`. Verified with 6 live curl tests: legit traffic → 200, no Origin → 403, evil Origin → 403, oversized message → 400, too many messages → 400, `max_tokens: 5000` capped to 500. Upstash rate limiting (Layer 3) parked as optional polish.
- **2026-04-21 (night)** — Phase 2 security track closed. p2-5a: GCP Console Website restrictions added to the Firebase browser key (`getkidshub.com`, `*.getkidshub.com`, `*.vercel.app`, `localhost`, `127.0.0.1`) — leaked key in git history now unusable from unauthorized origins (commit `2dea7a6`). p2-5b: **critical finding** — Firestore rules were Firebase's default test rules, `allow read, write: if request.time < 2026-05-06`, meaning the entire database was world-readable and world-writable until May 6. With the project ID already exposed in git, this was a one-line exfiltration script from total data loss. Wrote tightened `firestore.rules` at repo root, published to Firebase Console: users/{uid} gated to self with immutable role, centers/{ownerId} gated to matching owner, flat business collections gated to any authenticated owner via `isOwner()` helper, default-deny catch-all. Extensively documented pilot-era multi-tenancy caveat (Phase 3 p3-15 will tighten). Smoke-tested by logging in with freshly registered owner account and clicking every page — no permission errors (commit `cce5fe4`). Remaining p2 work: p2-7 (Vercel rename + domain).
- **2026-04-21 (late night)** — Phase 2 ✅. p2-7 closed: Vercel project reconfigured (renamed `kidshub-owner` → `kidshub-dashboard` in dashboard, kept the auto-assigned `kidshub-owner.vercel.app` URL since Vercel doesn't rename `.vercel.app` URLs on project rename — purely cosmetic). Added `kidshub-dashboard/vercel.json` with SPA rewrites (`/(.*) → /index.html`) and security headers (`X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`) plus `Cache-Control: public, max-age=31536000, immutable` on `/assets/*` (commit `2d36547`). Custom domain `dashboard.getkidshub.com` added in Vercel, GoDaddy CNAME pointed at `cname.vercel-dns.com`, DNS propagated under 10 min, SSL cert auto-issued. Owner login + dashboard smoke test green. **Subdomain choice (`dashboard.*`) free — no domain purchase, runs alongside the apex `getkidshub.com` landing on the same registrar.** Phase 2 complete; Phase 3 (`kidshub` Expo + RN Web rebuild) is next, biggest lift in the restructure.

- **2026-04-21 (late evening)** — Phase 2 kicked off. p2-1 folder rename (`kidshub-owner` → `kidshub-dashboard`, 58 files, history preserved) in commit `4988fcc`. p2-2 internal rename (package.json name, 13 mock staff emails scrubbed to `@example.com` per RFC 2606, lockfile rebuilt cleanly) in commit `7da147b`. p2-3 page audit done — 6 pages port to kidshub (Children, ChildProfile, Messages, Schedule, CheckIn, Activities), 5 stay in dashboard (Login, Register, Dashboard, Classrooms, Staff), Settings splits; matrix above drives Phase 3 (commit `85432ff`). p2-5 surfaced a git-history Firebase key leak (`810abbd`) — Firebase web keys aren't server secrets but needs GCP referrer restrictions + Firestore rules audit + optional rotation (dashboard work, tracked as p2-5a/b/c). p2-4 implemented the full role model (didn't exist — no central enum, no profile-doc subscription, no role-gated routing). Added `constants/roles.js` (ROLES + DASHBOARD_ALLOWED_ROLES), wired `AuthContext` to subscribe to `users/{uid}` via onSnapshot and expose `role` + role predicates, made `ProtectedRoute` take `allowedRoles` (default owner-only), built on-brand `Unauthorized` page distinguishing no-profile / wrong-role / unknown cases, switched `Register.jsx` to `ROLES.OWNER`. Dashboard builds clean (836kB bundle, pre-existing size warning). Client check is defense-in-depth — real gate is Firestore rules in p3-15.
